{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Create New Contact</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('contact') }}">Back to the contacts list</a>
        </li>
    </ul>
{% endblock %}