{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Create New Book</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('book') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
